apiVersion: 1

providers:
  - name: 'SMPP服务仪表盘'
    orgId: 1
    folder: 'SMPP服务'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards 